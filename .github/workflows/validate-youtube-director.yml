name: Validate YouTube AI Director

on:
  push:
    branches:
      - feat/youtube-ai-director

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.12.1
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @workspace/db exec tsc -p tsconfig.json
      - run: pnpm --filter @workspace/api-zod exec tsc -p tsconfig.json
      - run: pnpm --filter api-server typecheck
